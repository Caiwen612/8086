; MOV AH,0 ;CLEAR AH
; SUB AL,30H ;HEX CONVERT TO DEC
; DEC AL
; MOV SI,AX
; ;CHECK WHICH VALUE SHOULD BE TAKE FROM ARRAY AND DISPLAY(DEBUG)

; MOV AH,02H
; MOV DL,FOOD_PRICE[SI]
; ADD DL,30H ;DEC CONVERT TO HEX
; INT 21H






; ;PROMPT FOR USER INPUT FOOD QUANTITY
;  QUANTITY:
;  MOV AH,09H
;  LEA DX,F_CHOOSE    
;  INT 21H
; ;CHECK USER QUANTITY
;  MOV AH,01H
;  INT 21H
;  MOV I_QUANTITY,AL


; CMP F_CHOOSE,1 
; JE BURGER
; JNE 

; BURGER:
;     ADD SUM,5
;     JE QUANTITY


;  MOV AH,09h
;  LEA DX,F_QUANTITY    
;  INT 21H
; ;GET USER QUANTITY
;  MOV CX,3
;  MOV SI,0
;  GETQTY:
;     MOV AH,01H
;     INT 21H
;     MOV I_QUANTITY[SI],AL
;     INC SI
;     LOOP GETQTY
; ;